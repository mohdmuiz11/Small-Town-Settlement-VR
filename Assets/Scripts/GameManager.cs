using UnityEngine;
using UnityEngine.Events;
using UnityEngine.InputSystem;

/// <summary>
/// Management of game stuff when it is needed. Everything else is via GridSystem
/// </summary>
public class GameManager : MonoBehaviour
{
    private void Start()
    {
        Debug.Log("Game manager lol");
    }
}
